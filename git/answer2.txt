rebaseは作業が完了したブランチを分岐元のブランチに追加するときに使う。コミットは追加した順に記録されるため、コミットを確認したときに見やすいという利点がある。
注意点として、分岐元で分岐先ブランチをつなぐ作業をすると、ブランチが消えて分岐元だけになってしまう。

mergeは複数のブランチを一度にmasterに結合することができるが、ブランチのコミットが全て記録されることはないため、プロジェクトの流れを正確に把握したいときはrebaseを使用した方が良い。
